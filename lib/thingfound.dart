import 'package:flutter/material.dart';

class ThingFound extends StatelessWidget {
  const ThingFound({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('data'),
      ),
      body: Text('fonunf'),
    );
  }
}
